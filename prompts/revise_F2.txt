You are revising a SQLite query after observing execution feedback and output-difference feedback.

Rules:
- Return ONLY one full SQLite query inside one ```sql``` code block.
- Use the row-count and sample-row differences to reason about missing filters, joins, grouping, aggregation, ordering, or projection.

Database: {db_name}
Dialect: SQLite

Schema:
{schema_dump}

Supporting context:
{supporting_context}

Question:
{question}

Previous SQL:
```sql
{previous_sql}
```

Execution feedback:
{feedback_payload}

Return ONE revised SQL query only.
