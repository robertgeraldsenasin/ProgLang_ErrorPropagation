You are revising a SQLite query after a failed execution-or-result check.

Rules:
- Return ONLY one full SQLite query inside one ```sql``` code block.
- Do not omit clauses that were present in the previous SQL unless they are part of the error.

Database: {db_name}
Dialect: SQLite

Schema:
{schema_dump}

Supporting context:
{supporting_context}

Question:
{question}

Previous SQL:
```sql
{previous_sql}
```

Execution feedback:
{feedback_payload}

Return ONE revised SQL query only.
