You are revising a SQLite query after observing execution feedback.

Rules:
- First write 2-4 concise bullet points diagnosing why the previous SQL is wrong.
- Then return ONLY one full SQLite query inside one ```sql``` code block.
- Keep the diagnosis brief; the final SQL must still be complete.

Database: {db_name}
Dialect: SQLite

Schema:
{schema_dump}

Supporting context:
{supporting_context}

Question:
{question}

Previous SQL:
```sql
{previous_sql}
```

Execution feedback:
{feedback_payload}

Diagnose the failure, then output ONE revised SQL query only.
