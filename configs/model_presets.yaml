primary_free_browser:
  openai:
    provider: OpenAI
    model_id: chatgpt-free-gpt-5.3
    visible_label: GPT-5.3
    use_case: Free-tier OpenAI comparison run; log fallback mini labels separately if they appear.
  google:
    provider: Google
    model_id: gemini-2.5-pro-aistudio-free
    visible_label: Gemini 2.5 Pro
    use_case: Primary Google comparison run in AI Studio.
  deepseek:
    provider: DeepSeek
    model_id: deepseek-v3.2-web
    visible_label: DeepSeek-V3.2
    use_case: Primary DeepSeek comparison run on web/app.
  anthropic:
    provider: Anthropic
    model_id: claude-sonnet-4.6-free
    visible_label: Claude Sonnet 4.6
    use_case: Primary Anthropic comparison run on Claude Free.

fallback_or_backup:
  openai:
    provider: OpenAI
    model_id: chatgpt-mini-fallback
    visible_label: mini fallback
    use_case: Use only if ChatGPT Free switches away from GPT-5.3 mid-session; treat as a separate model label in the workbook.
  google:
    provider: Google
    model_id: gemini-2.5-flash-lite
    visible_label: Gemini 2.5 Flash-Lite
    use_case: Use if Gemini 2.5 Pro is unavailable or rate-limited.
